name: Build NJC APK

on:
  workflow_dispatch:
  push:
    paths:
      - 'twa/**'
      - '.github/workflows/build-apk.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Prepare optional release keystore
        working-directory: twa
        env:
          KEYSTORE_FILE_BASE64: ${{ secrets.KEYSTORE_FILE_BASE64 }}
        run: |
          if [ -n "$KEYSTORE_FILE_BASE64" ]; then
            printf '%s' "$KEYSTORE_FILE_BASE64" | base64 --decode > jayathasoft.keystore
          fi

      - name: Build debug APK
        working-directory: twa
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: njc-debug-apk
          path: twa/app/build/outputs/apk/debug/app-debug.apk

      - name: Build signed release APK
        if: ${{ secrets.KEYSTORE_PASSWORD != '' && secrets.KEYSTORE_FILE_BASE64 != '' }}
        working-directory: twa
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon

      - name: Upload signed release APK
        if: ${{ secrets.KEYSTORE_PASSWORD != '' && secrets.KEYSTORE_FILE_BASE64 != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: njc-signed-release-apk
          path: twa/app/build/outputs/apk/release/app-release.apk
